{
  "name": "keyboard-layout",
  "version": "2.0.13-d",
  "description": "Read and observe the current keyboard layout on OS X.",
  "main": "./lib/keyboard-layout",
  "scripts": {
    "test": "node ./prebuild-ci",
    "install": "prebuild-install || node-gyp rebuild",
    "prebuild": "prebuild --all --strip --verbose"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ssbc/keyboard-layout-prebuilt.git"
  },
  "bugs": {
    "url": "https://github.com/atom/keyboard-layout/issues"
  },
  "licenses": [
    {
      "type": "MIT",
      "url": "http://github.com/ssbc/keyboard-layout-prebuilt/raw/master/LICENSE.md"
    }
  ],
  "dependencies": {
    "event-kit": "^2.0.0",
    "nan": "^2.0.0",
    "prebuild-install": "^2.4.1"
  },
  "devDependencies": {
    "jasmine-focused": "^1.0.4",
    "prebuild": "^7.0.0",
    "prebuild-ci": "^2.2.3"
  }
}
